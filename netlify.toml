[build]
  command = "npm run build"
  publish = "dist"

# Redirect /api/* if you later use a backend; leave as-is for now
[[redirects]]
  from = "/api/*"
  to = "https://<RENDER_BACKEND_URL>/:splat"
  status = 200
  force = true